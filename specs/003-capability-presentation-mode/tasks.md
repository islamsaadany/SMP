# Tasks: Presentation mode for a supporting function

- [ ] T001 `src/present.js`: extract `openDeckWith`; add `deckSlidesFn(fk)` and
      `openDeckFn(fk)`.
- [ ] T002 `src/group-render.js`: Present button on `renderFnPerformance`.
- [ ] T003 `src/shell.html`: wire `[data-present-fn]`.
- [ ] T004 Verify: open the deck for every function as its head, walk all
      slides, measure overflow at 1600×900, QA walk.
