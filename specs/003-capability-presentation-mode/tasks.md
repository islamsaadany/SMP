# Tasks: Presentation mode for a supporting function

- [x] T001 `src/present.js`: extract `openDeckWith`; add `deckSlidesFn(fk)` and
      `openDeckFn(fk)`.
- [x] T002 `src/group-render.js`: Present button on `renderFnPerformance`.
- [x] T003 `src/shell.html`: wire `[data-present-fn]`.
- [x] T004 Verify: open the deck for every function as its head, walk all
      slides, measure overflow at 1600×900, QA walk.
