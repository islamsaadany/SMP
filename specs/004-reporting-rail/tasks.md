# Tasks: The rail on a unit's My reporting

- [x] T001 `src/group-render.js`: extract `reportPillarPane`; replace the
      accordion in `renderReport` with the rail/pane split (shared selection,
      tallies, sub-lines, rail foot).
- [x] T002 `src/pageinfo.js`: `u_report` Info gains the rail bullet.
- [x] T003 Verify: enter figures via the rail as a custodian, tally advances,
      selection persists across Performance/Strategy/Report, QA walk.
